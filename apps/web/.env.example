# Base URL for the API. In dev, Vite proxies /api -> http://localhost:4000 (see vite.config.ts).
VITE_API_BASE_URL=/api
# Toggle the MSW mock layer (1 = mock, 0 = real API). Useful until the SQL Server DB is reachable.
VITE_USE_MOCKS=1
